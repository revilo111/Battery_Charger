# Battery_Charger
Changes the maximum charge percentage for Linux systems temporarily using TLP.

## Dependencies
This program works on Linux Ubuntu with all base extensions installed.
Additionally the TLP Package is required and can be installed with:
`sudo apt install tlp tlp-pd tlp-rdw` For more information visit [their Github](https://github.com/linrunner/TLP).

## How to install
